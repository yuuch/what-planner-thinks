# 路径序列与排序核心密码 (PathKeys)

几乎所有关于性能优化的面试题，最后都会绕回一个命题：“为什么加了索引，ORDER BY 或 GROUP BY 变快了？”
在执行引擎的表象逻辑看来似乎是常理，但如果要在拥有极大复杂组合的 PostgreSQL 代价优化内核（CBO）中完美统一地实现这一机制，就需要一种能够**统一度量并且伴随路径推演任意流转的通用通货**。

在 PG 18.1 中，这套跨越所有的关系代数操作、用来锚定和流转排序特征通货，名叫 **`PathKey` (路径键)**。相关代码的核心座落于 `src/include/nodes/pathnodes.h` 和 `src/backend/optimizer/path/pathkeys.c`。
不懂 `PathKeys` 的设计与传播机理，你就无法真正看懂优化器在 `add_path` 里做出的那些惊心动魄的剪枝取舍。为何某些代价极高的“破烂”执行计划，反而会在复杂查询的最后关头被 PG 奉为至宝？

---

## 1. 结构起底：什么是 PathKey？

优化器在评估物理路径（比如一次走索引的扫描、或者哈希连接）时，不仅看“花了多大 Cost”、“能吐多少 Rows”，还必须精确标注出这股数据流**携带的元素排序特征**。

这套特征在源码中由 `List *pathkeys`（链表）承载。其基础粒子组件 `PathKey` 如下：
```c
typedef struct PathKey
{
    NodeTag     type;
    EquivalenceClass *pk_eclass;   /* 极其震撼：它不指向具体的列，而是指向一个覆盖全部域的等价类！ */
    Oid         pk_opfamily;       /* 排序操作符族 (如 B-tree opfamily OID)，指定排序规则 */
    int         pk_strategy;       /* 升序 ASC 还是 降序 DESC */
    bool        pk_nulls_first;    /* NULL 值是悬浮最前还是垫底 */
} PathKey;
```

### 1.1 为什么主体必须包裹 `EquivalenceClass`？
在上一个附录《等价类 (EC)》中埋下过伏笔：PG 极度高超的设计使得 `PathKey` 的最核心属性 `pk_eclass` 直接硬链接在了 `EquivalenceClass` 上。

试想场景：`SELECT t2.info FROM t1 JOIN t2 ON t1.id = t2.uid ORDER BY t2.uid;`
在优化器构建最底层时，系统发现 `t1` 表的 `id` 有 B-Tree 索引。于是探索出极速路径 `IndexScan(t1)`。这条底层路生成的初始 `pathkeys` 护照上骄傲地写着：“我送出的源头数据是严格基于 `EquivalenceClass{t1.id, t2.uid}` 排序的！”

当数据流抵达上层、需要执行全局决算的 `ORDER BY t2.uid` 时：
假若当年仅仅用字符串列名映射作为载体（如：`pk_var = t1.id`），那么顶层节点拿起客户要求的 `t2.uid` 一碰：“天啊，底层是 t1.id，客官要的是 t2.uid，给我退回！” 于是迫使几百万数据强行扔进 `Sort` 内存重排栈，引发算力海啸。

**但是**，正因为 PG 的 `PathKey` 绑死的是极度抽象化的等价类！
顶端审判官只需拿 `t2.uid` 代表的 EC 句柄，和底层的那个 EC 指针一碰。刹那间同气连枝完美吻合 (那正是因为 `t1.id = t2.uid`，起初就被合二为一)。判定官放行，极其平滑地帮引擎省切掉了最昂贵的重排局！

---

## 2. 需求侧大一统：规划器的排序货币化

在嵌套复杂的 SQL 分析大河中，混杂了无数试图干预排序的操作符：
* 终端发出了 `ORDER BY a, b` 要求顺位。
* 聚集指令下了 `GROUP BY c, d` 在启动 GroupAgg 前要求聚类有序。
* 过滤节点加了 `SELECT DISTINCT e` 企图通过排序来实现滑动去重。
* 大名鼎鼎的 `Merge Join` 强迫两营输入流按等同键绝对序列接壤。

对于老手工作坊式引擎，可能需要为以上所有情况各自手编一套“排序对账单”。
但在 PG 那神级铺陈的 `pathkeys.c` 架构中，所有这种排序请求，无论初衷为何，都会在预检阶段被诸如 `make_pathkeys_for_sortclauses` 的函数接管，彻彻底底**等价兑换为唯一标准流通物——`PathKey List` 链表。**

自大优化网络全面铺开的那一顷刻，优化器这个中央智脑，再也听不懂所谓的 ORDER BY 或 GROUP BY。它在判断下层分支能否直接插接时，唯一遵循的就是查验 `pathkeys` 国书。

---

## 3. 供给端长河：PathKeys 的诞生、传承与覆灭

面对这条流转下的继承物 `pathkeys`，不同物理计划节点态度截然不同：

### 3.1 源氏的血脉：底层索引扫描
最纯粹的 `pathkeys` 孕育自 B-Tree。定位到 `indxpath.c` 内部的 `build_index_pathkeys`：
当检索刺入索引结构时，PG 产生出一个拼接的衔接 `PathKey` 队列，赋予在即将上抛的 `IndexPath` 头上。如果是走路线粗犷的 `SeqScan`，生成的归口登记簿对于 `pathkeys` 则是一个干脆的 `NIL` (完全空指针)，宣告这是一堆无序的散沙。

### 3.2 屠夫：Hash Join 的破壁
当你汇入双表关联：
性能王者 **Hash Join** 的作风极其粗野。它压根不顾及底层是不是辛苦排好序的。它的底盘是将小表倒进哈希池开桶，随即拿大数据集激化碰撞。碰撞出水瞬间，所有的历史排序规律全盘崩散。
因此任何基于 `hash_inner_and_outer` 衍生的路径，它的光环继承簿 `path->pathkeys` 必定被无情洗白清零成为 `NIL`。

### 3.3 恪礼的承传者：NestLoop Join
**嵌套循环联合 (NestLoop)** 恪守着外表驱动一再往复过滤匹配的准则。
外环本有的序列起伏节奏，在吸纳内表后竟然绝妙地没有丝毫偏差！因此优化器会温柔地调用 `build_join_pathkeys`，把外表（Outer Rel）持有的金字令箭原封不动、极其虔诚完整地誊抄接手下来！

---

## 4. 终界审判：修剪免死金牌 (Path Pruning)

这也是隐藏于代价运算机制底下最为反转的绝魂之跃。
前文提过：`add_path()` 是一座极度现实的达尔文“裁决法庭”。如果不带排序加成，一条起步慢、总价高昂的路，百分百会被当场遗弃清盘！

**但是！** `PathKeys` 的设计存在，作为优化器世界的最高精神免死金牌，超降维介入干涉了丛林淘汰法则！

我们在极其严密的 Cost 较量之前，会看到如圣言般的预演过滤：`pathkeys_contained_in(path1->pathkeys, path2->pathkeys)`。

**一出震撼的剧本模拟**：
* 路径 A 候选：采用了一套笨重到令人发指的 SeqScan 自己全强制做重排序，开机代价大、总跑算价高昂至上千万！但自带了飘扬顺气的 `pathkeys = (t1.uid)` 序列大旗。
* 路径 B：一条能让算力尖叫绝世闪电的 Hash Join 超短通。起步快，全跑完不过区区大几千的消耗！但其身如恶兽全无秩序，`pathkeys == NIL`。

在没有神圣豁免的高维排序非闭包界中，这路 A 小丑绝必立刻暴死；新贵 B 将扫平一切！
但！在 PG 那深不可测的总决策眼界里，如果裁断虽然感觉 A 烂如破泥，**但是它顶头上那副全维的列旗却死超囊括了那光秃和残旧的 B 所全不配所有的法度红利。** 哪怕 A 的纸面耗费远高千倍估算成本线，PG 的主引擎手此时都会赫然插进力保：活拉下这面破 A 强行封卷！并永作存档列留进那最重要的预备大底座。

**最高中枢为何要苦保下流之列？**
因为中枢老辣深沉，在那远不见底的高顶组装关隘，它可能遭遇十重极度变态的 `Merge Join` 强令，更甚至撞见最冷酷杀神终极底符 —— `LIMIT 1`。

在极端灭顶的洪荒恶劣要求下。那个自持着武功第一极速猛飙全图却无次序的 Hash Join，为了强硬生拼全图结果以抗排期对局，甚至会被逼到去开启巨型外部破落磁盘暂存归排而令总价飙出百合千万计而炸穿。
反观那被上帝强保下这带好顺行秩序之破胎分支 A！在瞬满交接关斩停下 `LIMIT 1` 诉求那一刻。凭那极其超前精准拿对序列之完美契合并极速掐流神力直接砍停，瞬间封神！
通过此一层维向特比的神来保护局体系法道，奠基于 PostgreSQL 历经数十载依然傲立于星空云端王者之无上统治地位！
